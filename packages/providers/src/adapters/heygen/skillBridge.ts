export function heygenSkillRouting() {
  return {
    avatarFirst: ["photo with video request", "personal presenter request", "consistent identity request"],
    videoSkill: "Use HeyGen for presenter-led videos. Do not call raw HeyGen HTTP endpoints."
  };
}
