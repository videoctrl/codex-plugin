# Run Folder Format

Each run folder stores the content object, idea, brief, draft package, verification, storyboard, script, shot list, timeline, assets manifest, provenance, variants, renders, scheduler handoff, and feedback.
