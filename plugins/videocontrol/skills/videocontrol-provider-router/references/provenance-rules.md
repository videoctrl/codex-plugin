# Provenance Rules

Record provider, model, prompt, job id, input assets, asset path or result URL, created time, and rights status for generated or imported assets.

Generated assets should be imported into a content object before timeline use. After import, refresh the project review file so the asset gets a stable `asset:` selection id.
